# Agent.md - 项目开发需求

> 本文件是本项目的开发需求基准（Single Source of Truth）。
> 任何开发任务开始前，先阅读本文件；需求变更时同步更新本文件。

- **项目名称**：wte
- **创建时间**：2026-09-17
- **文档版本**：v4.0

---

## 1. 项目概述

**What to Eat (wte)** 是一款解决"外卖选择困难症"的移动端APP。用户在犹豫下一餐吃什么时，通过记录外卖历史、偏好分析和智能推荐，快速做出决策。核心交互为转盘随机推荐，降低用户决策成本。

- **目标用户**：经常点外卖的上班族、学生等"选择困难"人群
- **核心价值**：记录外卖历史 → 分析口味偏好 → 推荐下一餐，解决"今天吃什么"的日常难题

## 2. 目标与非目标

### 2.1 目标（Goals）

1. 实现外卖记录功能，支持手动录入和美食标签分类
2. 实现基于历史记录的用户偏好分析（口味、频次、消费）
3. 实现转盘随机推荐，解决"今天吃什么"决策问题
4. 实现历史统计图表（周/月报），让用户感知数据价值
5. 实现预算管理和排除机制，提升实用性和推荐精准度

### 2.2 非目标（Non-Goals）

- **不对接外卖平台API**：美团/饿了么不开放订单API，手动记录+标签足够，后续版本考虑更新
- **不做拍照识别菜品**：图像识别准确率低已否决，改用自然语言解析替代（见 F-14）
- **不做饮食健康分析**：非MVP阶段重点，后续可扩展
- **不做多人拼单/家庭模式**：前期聚焦单人场景
- **不做社交分享/排行榜**：非核心功能，后续可扩展
- **不做协同过滤推荐**：需大量用户数据，冷启动阶段不适用
- **不做账号与云端同步（v1.1 范围外）**：当前单机数据量小，本地备份已覆盖数据安全诉求；账号体系规划至 v1.3

## 3. 目标用户与使用场景

### 3.1 目标用户画像

- **上班族**：工作日午餐/晚餐依赖外卖，选择困难
- **学生**：校园外卖高频用户，预算有限
- **独居青年**：做饭少、点外卖多，想了解自己的饮食习惯

### 3.2 典型使用场景

1. **决策场景**：打开APP → 点击转盘 → 获得推荐 → 决定吃什么
2. **记录场景**：点完外卖后 → 打开APP → 记录今天吃了什么（选标签+店铺+价格）
3. **查看场景**：好奇自己的饮食习惯 → 查看统计图表 → 发现偏好
4. **预算场景**：月底快超预算 → 查看消费统计 → 调整点餐策略

## 4. 核心功能需求

<!-- 待确认：每条需求建议包含 需求描述 / 验收标准 / 优先级(P0-P2) -->

### F-00 工程基线（已落地）

- **描述**：仓库具备版本控制能力与开发需求基准文件，可支撑后续功能开发。
- **验收标准**：
  1. 目录已初始化为 git 仓库，主分支为 `main`；
  2. 根目录存在 `Agent.md` 与 `.gitignore`；
  3. 仓库已关联 GitHub 远程并可推送。
- **优先级**：P0

### F-01 外卖记录（P0）

- **描述**：用户可手动记录每餐外卖信息，包括店铺名称、菜品、价格、口味标签、用餐时间。
- **验收标准**：
  1. 支持新增记录，必填：店铺名、价格、至少1个标签；选填：菜品名、备注
  2. 支持编辑和删除已有记录：列表卡片菜单可直接编辑/删除，编辑页顶部亦有删除入口
  3. 删除需二次确认，且统计图表随数据变更同步刷新
  4. 记录列表按时间倒序展示
- **优先级**：P0

### F-02 美食标签体系（P0）

- **描述**：预设多维度标签（口味：辣/清淡/酸甜/麻辣；类型：快餐/正餐/小吃/饮品/甜品；菜系：中餐/日料/韩餐/西餐/东南亚），用户也可自定义标签。
- **验收标准**：
  1. 系统预设不少于20个常用标签，按维度分组
  2. 支持用户新增/编辑/删除自定义标签
  3. 记录外卖时可快速选择标签（多选）
- **优先级**：P0

### F-03 转盘随机推荐（P0）

- **描述**：用户犹豫时，通过点击转盘，基于历史偏好随机推荐一个菜品类别或具体菜品。支持排除机制。
- **验收标准**：
  1. 支持转盘动画展示推荐结果（扇区数量/颜色由标签数据动态决定）
  2. 推荐逻辑基于近期标签频率加权随机（近期少吃的权重更高）
  3. 支持"今天不想吃XX"排除特定标签
  4. 推荐结果可一键记录为今日外卖
- **优先级**：P0

### F-04 历史记录与统计图表（P0）

- **描述**：以图表形式展示用户外卖历史，包括口味偏好分布、消费趋势、点餐频次等。
- **验收标准**：
  1. 支持周/月时间维度切换
  2. 口味标签分布饼图
  3. 消费金额折线图（日/周维度）
  4. 点餐频次柱状图
  5. 点击图表可下钻查看对应记录
- **优先级**：P0

### F-05 收藏夹（P0）

- **描述**：用户可收藏喜欢的店铺或菜品，方便快速记录和推荐。
- **验收标准**：
  1. 支持从历史记录中收藏店铺/菜品
  2. 收藏列表独立展示，支持搜索
  3. 新增记录时可从收藏夹快速选择
  4. 收藏内容参与推荐权重计算
- **优先级**：P0

### F-06 偏好分析与推荐引擎（P0）

- **描述**：基于用户历史记录分析口味偏好，为转盘推荐和统计图表提供数据支撑。
- **验收标准**：
  1. 统计各标签出现频率
  2. 计算近期（7天）vs 长期（30天）偏好变化
  3. 推荐时优先推荐近期频率低但长期偏好的标签（平衡尝新与偏好）
  4. 排除昨天吃过的同类（避免重复）
- **优先级**：P0

### F-07 预算管理（P1）

- **描述**：用户设置月度外卖预算，APP实时统计消费进度并提醒。
- **验收标准**：
  1. 支持设置/修改月度预算金额
  2. 首页展示当月消费进度条（已花/预算）
  3. 消费达80%、100%时提醒
  4. 历史月份预算执行情况对比
- **优先级**：P1

### F-08 排除机制（P1）

- **描述**：推荐时支持排除用户"今天不想吃"的类别，提升推荐满意度。
- **验收标准**：
  1. 推荐前可选择排除1-3个标签
  2. 排除选项展示近期高频标签供快速选择
  3. 排除后推荐结果不包含被排除标签的菜品
- **优先级**：P1

---

## 4.1 v1.1 版本需求

> v1.1 定位为「稳定补全版」：优先修复 MVP 遗留缺口，补齐记录效率，暂不引入账号与云端同步。

### MVP 遗留缺口（须在 v1.1 修复）

| 编号 | 问题 | 影响 |
| --- | --- | --- |
| D-01 | 收藏夹闭环断裂：`Shops` 表无写入入口，收藏页永远为空 | F-05 名义完成、实际不可用 |
| D-02 | 统计缺柱状图：仅有饼图与折线图 | F-04-4 验收未达成 |
| D-03 | 图表下钻未实现 | F-04-5 验收未达成 |
| D-04 | 推荐理由已生成但 UI 未展示 | 推荐说服力弱 |
| D-05 | 数据库无迁移策略（`schemaVersion = 1`，无 `onUpgrade`） | 后续迭代有数据丢失风险 |
| D-06 | 无数据备份/导出能力 | 卸载即丢失，用户信任风险 |

### F-09 收藏夹闭环（P0）

- **描述**：打通"记录 → 收藏店铺 → 快速复用"完整链路，修复 D-01。
- **验收标准**：
  1. 记录表单支持勾选"收藏此店铺"，保存记录时同步写入 `Shops` 表
  2. 收藏列表按店铺聚合，显示关联记录数与最近消费时间
  3. 新增记录时可从收藏列表快速带出店铺名
  4. 收藏店铺参与转盘推荐候选（可开关）
- **优先级**：P0

### F-10 统计补全：柱状图与图表下钻（P0）

- **描述**：补齐 F-04 缺失验收项，修复 D-02、D-03。
- **验收标准**：
  1. 新增点餐频次柱状图（按周/按日维度）
  2. 点击饼图任一扇区，跳转至对应标签的记录列表
  3. 点击折线图任一数据点，展示该日记录明细
  4. 点击柱状图任一柱，展示对应时段记录明细
- **优先级**：P0

### F-11 推荐理由展示（P0）

- **描述**：将推荐引擎已生成的 `reason` 呈现给用户，修复 D-04。
- **验收标准**：
  1. 转盘结果弹窗展示推荐理由文案
  2. 推荐理由区分场景：久未食用 / 均衡尝新 / 偏好命中
  3. 结果弹窗"去记录"按钮自动带出推荐菜名
- **优先级**：P0

### F-12 数据备份与恢复（P0）

- **描述**：支持本地导出与导入，修复 D-06，覆盖数据安全诉求。
- **验收标准**：
  1. 支持导出全部数据为 JSON 文件（记录/标签/收藏/预算）
  2. 支持通过分享或文件选择导入 JSON 恢复数据
  3. 导入时校验格式版本，不兼容时给出明确提示
  4. 提供"清空全部数据"入口，需二次确认
- **优先级**：P0

### F-13 数据库迁移策略（P1）

- **描述**：建立 drift schema 版本迁移机制，修复 D-05。
- **验收标准**：
  1. `MigrationStrategy` 补齐 `onUpgrade`，按版本号逐级迁移
  2. 迁移逻辑具备单元测试覆盖
  3. 文档记录每次 schema 变更与对应迁移步骤
- **优先级**：P1

### F-14 自然语言快速记录（P1）

- **描述**：输入一句自然语言自动解析出店铺、菜品、价格，替代已否决的"拍照识别"方案。
- **验收标准**：
  1. 输入形如"中午麦当劳巨无霸35块"可解析出店铺=麦当劳、菜品=巨无霸、价格=35
  2. 解析结果填入表单后用户可确认或修改
  3. 解析失败时回退到手动填写，不阻塞记录流程
- **优先级**：P1

### F-15 上餐一键复用（P1）

- **描述**：一键复制历史记录，解决高频重复录入痛点。
- **验收标准**：
  1. 记录列表与首页提供"再来一餐"入口
  2. 复制后自动带入店铺、菜品、价格、标签，用餐时间取当前
  3. 支持查看"最近点过的店铺"快捷列表
- **优先级**：P1

## 5. 非功能需求

- **性能**：首页加载 < 1s，转盘动画流畅（60fps），记录保存响应 < 500ms
- **兼容性**：支持 iOS 14+ / Android 10+
- **数据安全**：用户数据本地存储优先，云端同步需加密传输
- **离线能力**：核心功能（记录、推荐、统计）支持离线使用
- **可维护性**：模块化设计，业务逻辑与UI分离

## 6. 技术栈与约束

| 分类 | 内容 |
| --- | --- |
| 语言 | Dart 3.x |
| 框架 | Flutter 3.x |
| 状态管理 | Riverpod 2.x |
| 本地数据库 | drift（SQLite ORM，类型安全，响应式查询） |
| 配置存储 | shared_preferences（预算、设置等 KV 数据） |
| 图表 | fl_chart（饼图/折线图/柱状图，纯 Dart 实现） |
| 路由 | go_router（声明式路由，官方推荐） |
| 转盘动画 | CustomPainter + AnimationController（自绘引擎，60fps） |
| 构建/包管理 | pub (Flutter) |
| 部署环境 | App Store + Google Play |
| 硬约束 | 一套代码双端运行；离线优先架构；纯本地存储（MVP阶段） |

## 7. 系统结构

### 7.1 模块划分

```
wte/
├── lib/ 或 src/
│   ├── models/          # 数据模型（Record, Tag, Shop, Budget）
│   ├── screens/         # 页面（首页、记录、统计、收藏、设置）
│   ├── widgets/         # 通用组件（转盘、标签选择器、图表）
│   ├── services/        # 业务逻辑（推荐引擎、偏好分析、预算计算）
│   ├── database/        # 本地数据库操作
│   └── utils/           # 工具函数
├── assets/              # 静态资源
└── test/                # 测试
```

### 7.2 核心数据流

```
用户记录外卖 → 保存到本地DB → 偏好分析引擎计算标签权重
                                      ↓
                              推荐引擎读取权重
                                      ↓
                          转盘展示推荐结果
```

## 8. 接口与数据

### 8.1 核心数据模型

| 模型 | 字段 | 说明 |
| --- | --- | --- |
| Record | id, shopName, price, tags[], dishName, note, mealTime, createdAt | 外卖记录 |
| Tag | id, name, dimension(口味/类型/菜系), isSystem, createdAt | 美食标签 |
| Shop | id, name, address, isFavorite, tags[], createdAt | 店铺（收藏） |
| Budget | id, month, amount, createdAt | 月度预算 |

### 8.2 本地存储方案

- 使用 SQLite 存储结构化数据
- 标签预设数据通过 JSON 文件初始化导入

### 8.3 Schema 版本与迁移

当前 `schemaVersion = 1`。

| 版本 | 变更内容 | 迁移步骤 |
| --- | --- | --- |
| 1 | 初始版本：`tags` / `records` / `shops` / `budgets` 四张表 | 无（`applyMigrationStep` 中为空实现） |

**变更流程**：修改表结构 → `AppDatabase.schemaVersion` 加 1 → 在 `applyMigrationStep` 中新增对应 `case` → 补充 `test/migration_test.dart` 用例。
未登记的版本会抛 `StateError`，避免静默跳级导致数据损坏。

**备份文件格式版本**（`backupFormatVersion`）独立维护，与 schema 版本解耦，当前为 1。

## 9. 开发与协作约定

### 9.1 分支策略

- 主分支：`main`
- 功能分支：`feature/<需求名>`
- 修复分支：`fix/<问题名>`

### 9.2 提交信息规范

采用 Conventional Commits：

```
<type>(<scope>): <subject>

type: feat | fix | docs | style | refactor | test | chore | perf
```

### 9.3 质量门禁

提交前必须通过以下检查：

```bash
flutter analyze   # 静态分析，要求 0 issue
flutter test      # 单元测试，要求全部通过
```

发布前额外执行：

```bash
flutter build apk --release
```

## 10. 验收标准

<!-- 待确认：整体交付如何算"完成"，尽量可验证 -->

- [ ] 所有 P0 需求验收标准通过
- [ ] 质量门禁全部通过
- [ ] 文档（含本文件）已同步更新
- [ ] 无已知的 P0/P1 缺陷

## 11. 里程碑

- **M0（已完成）**：仓库基线建立（git 仓库 + Agent.md + GitHub/Gitee 远程）
- **M1（已完成）**：技术选型与项目骨架搭建（Flutter + Riverpod + drift + fl_chart）
- **M2（已完成）**：核心数据层（数据模型、本地数据库、标签预设数据）
- **M3（已完成）**：外卖记录 + 标签体系（F-01 + F-02，可记录和分类外卖）
- **M4（已完成）**：偏好分析 + 推荐引擎 + 转盘（F-03 + F-06，核心推荐功能可用）
- **M5（已完成）**：统计图表 + 收藏夹（F-04 + F-05，数据可视化）
- **M6（已完成）**：预算管理 + 排除机制（F-07 + F-08，完善体验）
- **M7（已完成）**：UI打磨、测试、发布
- **M8（已完成）**：Android 构建链路打通，APK 构建成功（24.9MB）
- **M9（已完成）**：收藏夹闭环 + 推荐理由展示（F-09 + F-11）
- **M10（已完成）**：统计补全（F-10，柱状图 + 图表下钻）
- **M11（已完成）**：数据安全加固（F-12 + F-13，备份恢复 + 迁移策略）
- **M12**：记录效率提升（F-14 + F-15，自然语言记录 + 一键复用）
- **M13**：v1.1 回归测试与发版

## 12. 开放问题

| 问题 | 影响 | 需谁决策 |
| --- | --- | --- |
| 自然语言解析采用本地规则还是接入 LLM API？ | 决定 F-14 的准确率、成本与离线可用性 | 技术负责人 |
| 收藏店铺是否默认参与转盘推荐候选？ | 影响 F-09 交互与推荐结果构成 | 产品负责人 |
| 何时把本地备份升级为系统分享（跨设备/防卸载）？ | 影响 F-12 的备份可用范围 | 产品负责人 |

---

## 附录：变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
| --- | --- | --- | --- |
| v1.0 | 2026-09-17 | 初始版本 | - |
| v2.0 | 2026-09-17 | 完善项目概述、目标用户、功能需求（F-01~F-08）、技术栈、系统结构、里程碑 | - |
| v3.0 | 2026-09-17 | 确定技术栈（Flutter + Riverpod + drift + fl_chart），移除摇一摇功能，更新F-03为纯转盘推荐 | - |
| v4.0 | 2026-09-19 | MVP 测试完成；新增 v1.1 版本需求（F-09~F-15）与遗留缺口清单（D-01~D-06）；更新里程碑 M8~M13 与非目标 | - |
| v4.1 | 2026-09-19 | M9 完成：F-09 收藏夹闭环、F-11 推荐理由展示；F-09-4 以「导入收藏店铺」实现转盘候选联动 | - |
| v4.2 | 2026-09-19 | M10 完成：F-10 柱状图与三图表下钻（下钻形式决策为底部弹层）；修正折线图时间轴倒序缺陷 | - |
| v4.3 | 2026-09-19 | M11 完成：F-12 本地备份导出/恢复/清空（决策为指定目录保存）、F-13 逐级迁移策略与单测；新增 8.3 Schema 版本与迁移记录 | - |
| v4.4 | 2026-09-19 | 记录列表新增卡片菜单（编辑/删除）与删除二次确认；统计图表改为随记录变更自动刷新；F-01 验收标准补全删除相关要求 | - |
